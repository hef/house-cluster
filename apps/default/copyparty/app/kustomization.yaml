apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - secret.sops.yaml
configMapGenerator:
  - name: copyparty-config
    files:
    - main.conf=./resources/main.conf
generatorOptions:
  disableNameSuffixHash: true
